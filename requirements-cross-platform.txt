# Cross-platform Python requirements for Arabic OCR Server

blinker==1.7.0
click==8.1.7
colorama==0.4.6
Flask==3.0.2
Flask-CORS==4.0.0
itsdangerous==2.1.2
Jinja2==3.1.3
MarkupSafe==2.1.5
numpy==1.26.4
opencv-python==4.9.0.80
packaging==23.2
pillow==10.2.0
pytesseract==0.3.10
Werkzeug==3.0.1

# Database drivers (install what you need)
# For PostgreSQL (cross-platform)
psycopg2-binary>=2.9.9

# For SQL Server (Windows only)
pyodbc>=5.1.0; sys_platform == "win32"

# Windows-specific dependencies
pywin32>=306; sys_platform == "win32"

# Optional: WSGI server for production
waitress>=3.0.0
